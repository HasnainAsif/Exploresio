import React, { Fragment } from 'react'

const CurrentStatus = props => {
    return (
        <Fragment>
            <h3 style={{color : 'blue',textAlign : 'center'}}>Tour is on the way...</h3>
        </Fragment>
    )
}

export default CurrentStatus
